#include <bits/stdc++.h>
using namespace std;

int main() {
	int N;
	cin >> N;
	int trib[N];
	trib[0] = 0;
	trib[1] = 0;
	trib[2] = 1;
	for (int i = 3; i < N; i++)
		trib[i] = (trib[i-1]+trib[i-2]+trib[i-3]) % 10007;
	cout << trib[N-1] << endl;
	return 0;
}
