#include <bits/stdc++.h>
using namespace std;

int main() {
	int N;
	cin >> N;
	set<string> str;
	for (int i = 0; i < N; i++) {
		string S;
		cin >> S;
		str.insert(S);
	}
	cout << str.size() << endl;
	return 0;
}
