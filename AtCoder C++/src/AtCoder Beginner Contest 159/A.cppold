#include <iostream>
using namespace std;

int main() {
	int N, M;
	cin >> N >> M;
	int cnt = 0;
	if (N >= 2)
		cnt += N * (N - 1) / 2;
	if (M >= 2)
		cnt += M * (M - 1) / 2;
	cout << cnt << endl;
	return 0;
}
