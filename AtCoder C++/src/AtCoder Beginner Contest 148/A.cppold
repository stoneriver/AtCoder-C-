#include <bits/stdc++.h>
using namespace std;

int main() {
	int A[2];
	cin >> A[0] >> A[1];
	sort(&A[0], &A[2]);
	if (A[0] == 2) {
		cout << 1 << endl;
	} else if (A[1] == 3) {
		cout << 2 << endl;
	} else {
		cout << 3 << endl;
	}
	return 0;
}
