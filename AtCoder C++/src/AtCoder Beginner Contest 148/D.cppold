#include <bits/stdc++.h>
using namespace std;

int main() {
	int N;
	cin >> N;
	int tgt = 1, a;
	for (int i = 0; i < N; i++) {
		cin >> a;
		if (a == tgt)
			tgt++;
	}
	if (tgt == 1)
		cout << -1 << endl;
	else
		cout << N - tgt + 1 << endl;
	return 0;
}
