#include <bits/stdc++.h>
using namespace std;

int main() {
	int H1, M1, H2, M2, K;
	cin >> H1 >> M1;
	cin >> H2 >> M2;
	cin >> K;
	int T1 = 60 * H1 + M1;
	int T2 = 60 * H2 + M2;
	cout << T2 - T1 - K << endl;
	return 0;
}
