// EX13 - 平均との差

#include<bits/stdc++.h>
using namespace std;

int main() {
	int N, avg = 0;
	cin >> N;
	int A[N];
	for (int i = 0; i < N; i++) {
		cin >> A[i];
		avg += A[i];
	}
	avg /= N;
	for (int i = 0; i < N; i++) {
		cout << abs(A[i] - avg) << endl;
	}
	return 0;
}
