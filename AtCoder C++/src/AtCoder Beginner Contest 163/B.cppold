#include <bits/stdc++.h>
using namespace std;

int main() {
	int N, M;
	cin >> N >> M;
	int A = 0;
	for (int i = 0; i < M; i++) {
		int Ai;
		cin >> Ai;
		A += Ai;
	}
	cout << (N - A < 0 ? -1 : N - A) << endl;
	return 0;
}
