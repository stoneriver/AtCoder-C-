#include <bits/stdc++.h>
using namespace std;

int main() {
	int K, A, B;
	cin >> K >> A >> B;
	for (int i = 1; K*i <= B ; i++)
		if (A <= K*i){
			cout << "OK" << endl;
			return 0;
		}
	cout << "NG" << endl;
	return 0;
}

