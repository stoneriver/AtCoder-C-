#include <bits/stdc++.h>
using namespace std;

int main() {
	int A, B, C;
	cin >> A >> B >> C;
	cout << ((A == B && B == C) || (A != B && B != C && C != A) ? "No" : "Yes")
			<< endl;
	return 0;
}
