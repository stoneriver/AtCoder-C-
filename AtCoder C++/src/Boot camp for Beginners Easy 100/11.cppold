#include<bits/stdc++.h>
using namespace std;

int main() {
	int N;
	cin >> N;
	for (int i = N; i > 0; i--) {
		int n = i;
		while (n % 2 == 0) {
			n /= 2;
		}
		if (n == 1) {
			cout << i << endl;
			break;
		}
	}
	return 0;
}
